name: raha_adsense
description: Flutter SDK for Raha banner and video advertising.
version: 2.0.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  dio: ^5.11.0
  flutter:
    sdk: flutter
  pretty_dio_logger: ^1.4.0
  url_launcher: ^6.3.1
  uuid: ^4.6.0
  video_player: ^2.9.2
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
